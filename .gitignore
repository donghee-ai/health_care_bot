# 의존성 - 113MB. web/package-lock.json 으로 정확히 재생성되므로 커밋하지 않는다
# (OS/아키텍처별 네이티브 바이너리가 섞여 있어 커밋하면 다른 환경에서 깨진다)
node_modules/

# 파이썬 캐시
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 대외비 - 멘토 패키지는 절대 커밋 금지 (현재 없지만 방어적으로)
docs/mentor/

# 로그 / 런타임 산출물
*.log
logs/

# 경비 모드 캡처 (사람 사진 + 로그) - 리포에 커밋하지 않는다
captures/

# OS / 편집기
.DS_Store
Thumbs.db
.vscode/
.idea/
